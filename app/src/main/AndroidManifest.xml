<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eric.proyectoqr">

    <!-- Permisos requeridos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Declaración de características de hardware (opcionales pero recomendables) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoQR"
        android:usesCleartextTraffic="true">

        <!-- LoginActivity (inicio de sesión) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProyectoQR">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (pantalla principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProyectoQR" />

        <!-- (Opcional) Tu actividad previa si planeas usarla -->
        <activity
            android:name=".ScannerActivity"
            android:exported="false"
            android:label="Escáner de Boletos"
            android:theme="@style/Theme.ProyectoQR" />

        <!-- NUEVO: Actividad de escaneo con CameraX + ML Kit -->
        <activity
            android:name=".ScanQrActivity"
            android:exported="false"
            android:label="Escanear QR"
            android:theme="@style/Theme.ProyectoQR" />

        <!-- NUEVO: Pantalla para mostrar el contenido del QR -->
        <activity
            android:name=".TicketInfoActivity"
            android:exported="false"
            android:label="Información del Boleto"
            android:theme="@style/Theme.ProyectoQR" />
    </application>

</manifest>
